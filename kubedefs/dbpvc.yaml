apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vprodb-pvc
  labels:
    app: vprodb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3 Gi
  storageClassName: default
      
